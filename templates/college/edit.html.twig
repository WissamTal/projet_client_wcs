{% extends 'base.html.twig' %}

{% block title %}Editer College{% endblock %}

{% block body %}
    <h1 class="text-center">Editer College</h1>
    <section class="m-5">
        {{ include('college/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('college/_delete_form.html.twig') }}
         <div class="btn btn-light text-end">
            <a href="{{ path('college_index') }}">Retour à la liste</a>
        </div>
    </section>
{% endblock %}
