<form method="post" action="{{ path('members_delete', {'id': legalPerson.id}) }}" onsubmit="return confirm('Voulez-vraiment supprimer cette personne ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legalPerson.id) }}">
	<button class="btn">Supprimer</button>
</form>
